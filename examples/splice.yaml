version: 1

rules:
  - between:
      interface: wasi:http/handler@0.3.0-rc-2026-01-06
      inner:
        name: srv-b
      outer:
        name: srv
    inject:
      - name: middleware-a
      - name: middleware-b
