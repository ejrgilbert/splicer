package test:wrapped;
interface store {
  resource bucket {
    constructor(name: string);
    get: func(k: string) -> option<string>;
    set: func(k: string, v: string);
  }
}
world provider { export store; }
