[package]
name = "splicer-builtin-protocol"
version = "0.2.0"
edition = "2021"
authors = ["Elizabeth Gilbert"]
description = "Internal crate for splicer. Host<->builtin wire contracts: manifest schema, section-name conventions, wire formats, and a build.rs codegen helper. Not intended for use outside the splicer ecosystem."
license = "Apache-2.0"
readme = "README.md"
repository = "https://github.com/ejrgilbert/splicer"
categories = ["wasm"]
keywords = ["wasm", "WebAssembly", "splicer"]

[workspace]

[dependencies]
serde = { version = "1", features = ["derive"] }
toml = "0.8"
wasmparser = "0.247.0"
proc-macro2 = "1"
quote = "1"

[dev-dependencies]
wasm-wave = { version = "0.247.0", default-features = false, features = ["std"] }
