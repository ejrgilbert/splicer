//! Typed (tier-3/4) test root. The fuzz submodule layers a
//! structural property test on top of the same generator entry
//! point the matrix tests exercise.

mod bindgen_contract;
mod matrix;
mod pipeline;
